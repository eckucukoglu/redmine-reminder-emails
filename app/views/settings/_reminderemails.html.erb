<fieldset>
  <legend><%= l(:reminder_emails) %> <%= l(:reminder_settings) %></legend>
  <p>
    <label><%= l(:settings_rake_path) %></label><%= text_field_tag 'settings[rake_path]', settings['rake_path'] %>
  </p>
  <p>
    <label><%= l(:settings_script_path) %></label><%= text_field_tag 'settings[script_path]', settings['script_path'] %>
  </p>
</fieldset>
